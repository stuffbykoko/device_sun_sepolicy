type hal_vibrator_ext_default, domain;
hal_server_domain(hal_vibrator_ext_default, hal_vibrator_ext)

type hal_vibrator_ext_default_exec, exec_type, vendor_file_type, file_type;
init_daemon_domain(hal_vibrator_ext_default)

set_prop(hal_vibrator_ext_default, sun_feature_prop)
